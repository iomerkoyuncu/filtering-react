import { createTheme } from "@mui/material";

export const theme = createTheme({
  typography: {
    fontFamily: "Inter",
  },
  palette: {
    background: {
      default: "#fff",
    },
    primary: {
      main: "#1a2327",
    },
    secondary: {
      main: "#30475E",
    },
    black: {
      main: "#1a2327",
    }
  },
});
